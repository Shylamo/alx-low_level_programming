#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Printing the alphabet in lower cases'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	
	for (i = 48 ; i <= 57 ; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
